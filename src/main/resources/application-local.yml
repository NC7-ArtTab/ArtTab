spring:
  config:
    activate:
      on-profile: local
  devtools:
    livereload:
      enabled: true
  #  h2:
  #    console:
  #      enabled: true
  #      path: /h2-console
  #      settings:
  #        web-allow-others: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:arttab;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    initialization-mode: always
#  schema: classpath:schema.sql
#  data: classpath:data.sql
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/studydb
#    username: study
#    password: 1111
sql:
  always:
    init: true
server:
  port: 8080
  shutdown: graceful