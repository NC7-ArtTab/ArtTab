<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="arttab.server.dao.FAQDao">
    <!-- FAQ 목록 조회 -->
    <select id="selectAllFAQs" resultType="arttab.server.vo.FAQ">
        SELECT * FROM faq;
    </select>

    <!-- FAQ 상세 정보 조회 -->
    <select id="selectFAQById" parameterType="java.lang.Long" resultType="arttab.server.vo.FAQ">
        SELECT * FROM faq WHERE faq_no = #{faqNo};
    </select>

    <!-- FAQ 생성 -->
    <insert id="insertFAQ" parameterType="arttab.server.vo.FAQ">
        INSERT INTO faq (mem_no, faq_title, faq_content, faq_writer, faq_reg_datetime, faq_status)
        VALUES (#{memNo}, #{faqTitle}, #{faqContent}, #{faqWriter}, #{faqRegDatetime}, #{faqStatus});
    </insert>

    <!-- FAQ 수정 -->
    <update id="updateFAQ" parameterType="arttab.server.vo.FAQ">
        UPDATE faq
        SET mem_no = #{memNo}, faq_title = #{faqTitle}, faq_content = #{faqContent}, faq_writer = #{faqWriter},
        faq_reg_datetime = #{faqRegDatetime}, faq_status = #{faqStatus}
        WHERE faq_no = #{faqNo};
    </update>

    <!-- FAQ 삭제 -->
    <delete id="deleteFAQ" parameterType="arttab.server.vo.FAQ">
        DELETE FROM faq WHERE faq_no = #{faqNo};
    </delete>
</mapper>
